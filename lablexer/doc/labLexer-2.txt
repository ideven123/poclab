由于不知什么原因平台上的代码忽然没了，而我之前也没有及时git push，所以现在上传的代码是在本地跑的版本 ，而不是测试的版本。区别在于（测试数据是从文件读入，而不是输入流读入）。
设计思路： 
  1.先了解了flex的词法规则。 构造能识别other ，relop并给出相应的处理。
  2.学习lex.yy.c文件的作用。 学会使用flex生产词法分析器的过程中产生的中间变量如：yytext，yyleng
  3.利用条件编译，生成两种情况的可执行文件。

  遇到的问题： getsym()函数的声明的处理。一开始会有一个冲突，因为有两个定义不同的getsym（）函数。
               后来就利用条件编译，在头文件里声明getsym()函数的声明的处理。一开始会有一个冲突，以为有两个定义不同的getsym（）函数。后来就利用条件编译，在头文件里声明getsym
